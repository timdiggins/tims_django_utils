from distutils.core import setup
setup(
    name = 'tims_django_utils',
    packages = ['tims_django_utils'],
    version = '0.1',
    description = 'A bunch of django utils, mostly for testing',
    author='Tim Diggins',
)
